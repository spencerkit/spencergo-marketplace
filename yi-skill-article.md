# 周易占卜遇上 Claude Code：阿二的江湖故事

## 第一章：风雪夜的困局

那是一个风雪夜。

大龄侠客阿二在黑夜里独自前行。行情不好，天天想着跑路。天天想着一夜暴富。这就是他的江湖。

雪还在下。

阿二想过很多办法。投靠新的帮派、学新的剑法、找新的靠山。但这些都太慢了。快意恩仇的江湖里，没人有时间等待。

干脆算一卦吧。

传统做法是找个大师，摇铜钱，起卦象。问题在于：要上班，没时间，路太远，大师难找。而且说实在的，玄学这东西，说有用也有用，说没用也没用。

阿二灵机一动。既然都是玄学，让 Claude Code 帮他算也一样嘛。

它懂算法，懂数据，懂 AI。摇铜钱、算卦象、调用大模型解读——一条龙服务。

与其等大师，不如等代码。与其信命运，不如信 AI。

这就是阿二的江湖。

故事才刚开始。当他真正开始动手的时候，才发现这背后藏着更大的秘密。

## 第二章：Claude Code 的超能力——Plugin 和 Skill

阿二用 Claude Code 已经有一段时间了。一开始，他以为它就是一个普通的代码助手。

后来才发现，这个工具比他想象的要深。

就像江湖上的高手，表面看起来平平无奇，但一旦出手，就能翻江倒海。Claude Code 也是这样。它有一套隐藏的体系——Plugin 和 Skill。

**Plugin 是什么？**

简单说，Plugin 是 Claude Code 的"武器库"。你需要什么功能，就装什么 Plugin。Git 集成、数据库支持——都是官方提供的标准配置。

问题在于，这些 Plugin 太"通用"了。一把剑再锋利，也就砍个东西。但江湖高手需要的是"专属"武器——按自己风格、需求量身定做的兵器。

这就引出了 Skill。

**Skill 是什么？**

Skill 是 Claude Code 的"秘籍"。不是官方提供的通用工具，而是你自己定义的能力。写作、代码审查、占卜——什么都能做。

Plugin 是"工具"，Skill 是"绝学"。Plugin 让 Claude Code 能做什么，Skill 让它知道怎么做。

阿二明白了。要算卦，他需要的不是通用的 Plugin，而是一个专属的 Skill——yi Skill。

故事这才真正开始。

## 第三章：技术拆解——摇铜钱的秘密

现在到了最关键的时刻。

一个 Skill 要有真本事。yi Skill 的真本事就是：摇铜钱。

**摇铜钱的秘密**

传统摇铜钱：三枚铜钱，摇六次。每次根据正反面组合得到一个爻。六个爻组成一卦。

听起来简单，背后的逻辑却深。

三枚铜钱，两种可能，一次摇下来有 2³ = 8 种可能。周易里，这 8 种可能分成 4 种结果：

- 三个正面 = 老阳（动爻）✦
- 两个正面一个反面 = 少阳（阳爻）—
- 一个正面两个反面 = 少阴（阴爻）- -
- 三个反面 = 老阴（动爻）✦

看起来玄妙，其实就是概率问题。yi Skill 用代码模拟这个过程。

`divination.js` 里用 `Math.random()` 生成随机数，模拟摇铜钱。每次摇，生成三个 0 到 1 之间的随机数，判断正反。六次摇，六个爻，一卦出来。

就这么简单。

**卦象的计算**

光有数字不够。摇出来的只是一个数字序列。要变成真正的卦象，需要查表。

yi Skill 内置两个数据文件：`8-gua.json` 和 `64-gua.json`。

`8-gua.json` 存八卦基本信息：乾、兑、离、震、巽、坎、艮、坤。每个卦有符号、自然象征、阴阳属性。

`64-gua.json` 存六十四卦完整数据：卦名、卦辞、爻辞、六亲、世应。这是周易的核心知识库。

摇出一卦后，yi Skill 根据六个爻的组合，在 `64-gua.json` 里查找对应卦象。把卦辞、爻辞、象征意义提取出来。

"卦象翻译机"。输入随机数，输出完整解读。

**AI 的魔法**

这还没完。

真正的魔法在 AI 解读这一步。

摇出卦象，yi Skill 生成一个精心设计的提示词，丢给大模型。大模型根据提示词，结合用户问题、卦象含义、爻辞解释，生成占卜解读。

这一步很关键。卦象是死的，AI 的解读是活的。能根据你的具体情况，给出针对性建议。

就像高手的剑法，不在剑有多锋利，而在于怎么用剑。

## 第四章：代码亮点——设计的巧妙之处

代码本身不复杂。复杂的是怎么设计。

就像剑客，剑法再精妙，也得有一套独特招式才能成为高手。yi Skill 的招式是什么？

**动爻的计算——剑法的变化**

yi Skill 最巧妙的地方是对"动爻"的处理。

传统周易里，动爻很重要。动爻会变成另一个爻，产生"变卦"。一个卦可以变成另一个卦，这就是"主卦"和"变卦"的关系。

怎么判断哪个爻是动爻？答案在四种摇铜钱的结果里：老阳和老阴才是动爻。

代码里用一个简单判断搞定：如果是老阳或老阴，就标记为动爻。根据动爻位置，自动计算变卦。

看起来简单，背后的逻辑却深。这涉及周易的核心——变化。一切都在变，唯有变不变。

**提示词的设计**

另一个亮点是提示词。

yi Skill 生成的提示词不是简单的"请解读这个卦象"。而是精心设计的完整上下文，包含用户问题、卦象信息、爻辞解释。

让大模型有足够的信息做出更准确的解读。就像找大师算卦，大师不仅看卦象，还要听问题、了解背景。yi Skill 做的是同样的事。

**数据的组织**

还有个细节是数据组织。

`64-gua.json` 里的数据不是随意堆砌的。每个卦都有卦名、卦辞、爻辞、六亲、世应。数据的组织方式直接影响查询效率和准确性。

yi Skill 用的是简单但有效的方案：用卦的编号作为 key，直接索引。查询时不需要遍历整个数据库，O(1) 时间复杂度。

小细节，但这是工程思维和玄学思维的碰撞。

## 第五章：应用想象——yi Skill 的无限可能

现在 yi Skill 已经能算卦了。故事还没完。

**当下的应用**

yi Skill 能做什么？

最直接的就是占卜。在 Claude Code 里输入 `/yi`，然后输入问题。yi Skill 帮你摇铜钱、起卦象、生成解读。

对江湖人士有什么用？说实话，可能没什么实际用处。但重点不在这里。重点是，这证明了一个可能性——Claude Code 可以做任何事情，只要想得到。

**未来的想象**

往深了想，空间就大了。

可以把 yi Skill 和其他 Skill 结合起来。写作 Skill 根据卦象生成文章。命名 Skill 根据卦象给项目起名。甚至创建"决策 Skill"，重大技术决策前先问一卦。

或者扩展到其他占卜体系。塔罗牌、八字、面相……只要想得到，都可以用同样的思路实现。

再往深了想，yi Skill 在探索一个更大的问题：怎么让 AI 理解和处理非结构化的、充满象征意义的信息？

周易的卦象就是这样的信息。不是数字，不是代码，而是一种古老的、充满智慧的符号系统。yi Skill 做的，是把这个符号系统翻译成 AI 能理解的语言。

这个思路可以应用到很多地方。比如，让 AI 理解艺术、文化、人性？

## 第六章：总结——开源邀请和后续规划

故事讲到这里，该收尾了。

但在收尾之前，阿二想说一句：这一切都是开源的。

代码在 GitHub 上，任何人都可以看、可以用、可以改。这不是阿二一个人的项目，而是一个邀请——邀请你加入这个实验。

**怎么使用？**

首先，在 Claude Code 里添加 spencergo-marketplace：

```bash
/plugin marketplace add spencerkit/spencergo-marketplace
/plugin install yi@spencerkit/spencergo-marketplace
```

然后，输入 `/yi`，选择问题类型或自己输入问题。yi Skill 帮你摇铜钱、起卦象、生成解读。

**你可以做什么？**

发现 bug，或者有更好的想法，欢迎提 PR。想基于 yi Skill 的思路创建自己的 Skill，也欢迎。

这就是开源的魅力。一个人的想法，可以变成很多人的创意。

**最后的话**

回到开头的梦想——暴富。

yi Skill 能帮阿二暴富吗？说实话，可能不能。但它能帮他做一件更重要的事——理解这个世界。

周易说，"一阴一阳之谓道"。代码也是这样，0 和 1，阴和阳。两者是相通的。

所以，与其等大师，不如等代码。与其信命运，不如信 AI。

但最重要的是相信自己。

最好的占卜，就是行动。

那个风雪夜，阿二明白了。这不只是技术项目，而是人生的启示。

在江湖上，每个人都需要找到属于自己的绝学。

yi Skill，就是阿二的绝学。

---

**字数统计**：约 2000 字
**阅读时间**：约 9 分钟
